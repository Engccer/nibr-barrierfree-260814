# CLAUDE.md

> 이 파일은 2026년 8월 14일 국립생물자원관 교육강사 워크숍 강연 덱을 다룰 때 참고하는 지침이다.

## 이 덱

| 항목 | 내용 |
|---|---|
| 강의 제목 | 내가 만난 배리어프리 교육과 전시 |
| 행사 | 국립생물자원관 교육강사 워크숍 |
| 일시 | 2026년 8월 14일(금) 11:00~12:00 (60분) |
| 청중 | 20명 (교육팀 직원 8명, 교육강사 12명) |
| 계보 | **visual** (사진·영상 중심. 할 말은 원고에 있고 현장에서 구두로 푼다) |
| 슬라이드 | 46장 |

원고는 `G:\내 드라이브\KHY\Lectures\260814 국립생물자원관 교육강사 워크숍\김헌용_강의원고.md`가 정본이다. 콘텐츠 자산(원고·공문·설문·참고자료)은 그 행사 폴더에 두고, 덱만 여기에 둔다.

## 구성

| 구간 | 슬라이드 | 성격 |
|---|---|---|
| 들어가며 | 1~4 | 표지, 목차, 자기소개 영상, 세상과 소통하는 두 가지 방식 |
| 1장 배제하는 가이드와 포용하는 가이드 | 5~16 | 뮤지엄 산, 반 고흐 미술관, 국립중앙박물관 |
| 2장 촉각 경험의 의미 | 17~27 | 건축 모형, 경기장 소장품, 「공간_사이」, 점자 카드 |
| 3장 한 학생을 위한 설계 | 28~35 | 설계 단계의 접근성, UDL, 앱 두 개, 설문 |
| 4장 다섯 가지 | 36~42 | 표지, 제언 한 장씩, 다시 쓰인다 |
| 나가며 | 43~46 | 인프라 논지, 마지막 문장, 링크 |

## 화면에 두는 것과 두지 않는 것

**슬라이드는 원고의 요약이 아니다.** 세부 설명과 개인적 소회는 발표자가 구두로 풀고, 화면에는 제목과 소제목과 키워드와 핵심 메시지만 둔다. 원고를 요약해 옮기면 대본이 되고 청중은 화면과 말을 따로 처리하게 된다.

- **목록은 문장으로 쓰지 않는다.** 명사형이나 음슴체로 끊는다. 대부분은 `.chips`로 두는 편이 낫다
- **칩은 `<ul class="chips">` 안의 `<li class="chip fragment">`다.** `<span>`으로 두거나 가로로 늘어놓지 않는다(아래 접근성 항목)
- **1장은 사진과 영상이 무엇인지만 전한다.** 설명 목록을 얹지 않는다. 그 자리는 발표자가 화면을 보여 주며 말로 채운다
- **`.point`(핵심 메시지)는 문장으로 둔다.** 명사형 규칙은 불릿에만 적용된다
- **인용은 온전히 하거나 아예 버린다.** 원고 문장의 뒷부분만 떼어 오면 주어나 서술이 빈 자리가 생기고, 가리킬 대상이 슬라이드에 없는 「그 차이」·「그 예」가 남는다. 앞부분까지 가져와 문장을 완성하거나, 원고 워딩을 버리고 메시지만 새로 쓴다. 2026-08-14에 4·6·29·31·38·39장이 이 기준으로 고쳐졌다. 4장은 「그 차이」가 가리킬 대비 문장을 결론 바로 앞에 세우는 방식으로, 39장은 원고 워딩을 버리고 장면을 그대로 적는 방식으로 풀었다
- 사진 전면 슬라이드는 제목과 캡션만 곁들인다. 설명이 필요하면 다음 슬라이드로 나눈다(8·9장 나탈리, 20·21장 점자 지도, 23·24장 경기장, 25·26장 공간_사이)

## 4장 다섯 제언은 원고 문장을 옮기지 않는다

원고 4장은 평서체 설명문이고 슬라이드는 청중이 교육실에서 그대로 할 수 있는 지시문이다. 이 구간만 계보 규칙(원고 문구를 가져온다)에서 벗어나며, 그래서 37~41장은 `.deck-check.json`의 `skip`에 들어 있다.

37~41장은 한 장에 하나씩, 제목이 곧 행동 문장이다(`.act`). 「시작 전에 묻는다」가 아니라 「교육 시작 전에 먼저 물어보세요」로 쓴다. 뒷받침하는 `.chips`에는 그 자리에서 할 수 있는 구체적 방법만 둔다.

## 청중이 하는 일을 잘못 부르지 않는다

이 강연의 청중은 도슨트나 전시 해설사가 아니다. 국립생물자원관 **교육실**에서 유아·초중고·특수교육 대상 단체를 맞아 **「생물다양성교실」**을 진행하는 전문 교육강사다. 도입 영상, 전개, 만들기·보드게임·표본 관찰 활동으로 이어지는 구성이고, 시간은 유아 60분, 초중고 90분, 특수 50분이다. 전시관 관람은 별도 예약이라 교육과 분리돼 있다.

따라서 **「전시 교육」·「관람객」으로 그들의 일을 부르지 않는다.** 그들의 대상은 학생이다. 다만 내 관람 경험을 서술하는 1장과 2장에서 나 자신을 「관람객」이라 부르는 것은 그대로 둔다. 근거는 `참고자료\`의 2026년 하반기 안내서 3종이며, 2026-08-14에 이 프레이밍으로 원고와 덱을 함께 고쳤다.

## 자산 출처

| 파일 | 출처 |
|---|---|
| `img/` 반 고흐·국립중앙박물관·촉각 모형 12장 | 행사 폴더 `사진·영상\`. 긴 변 1600px, JPEG 82로 변환 |
| `img/gonggan-sai*.jpg` | 뉴시스 2025-03-24 보도 사진. **슬라이드에 출처를 표기했다** |
| `img/stadium-boot.jpg` | Gemini(`gemini-3.1-flash-image`)로 생성. 현장 사진을 구하면 교체한다. 슬라이드에 생성 이미지임을 밝혔다 |
| `video/` 4종 | 행사 폴더 원본을 1280px·CRF 26으로 재인코딩 |
| 33·34장 설문 차트 | 260523 기조강연 덱의 마크업 구조를 가져오고 표본을 **155명 기준**으로 갱신했다. 스타일은 이 덱 팔레트로 새로 썼다 |

설문 수치는 원고 표가 정본이다. 130명은 260523 강연 당시 기준이므로 이 덱에 섞지 않는다.

## 검증

```bash
python "C:\Users\pc\.claude\skills\lecture-deck\scripts\check_all.py" "C:\Users\pc\Windows-Projects\education\lecture-decks\260814 국립생물자원관 교육강사 워크숍"
```

원고 경로·계보·예외 슬라이드는 `.deck-check.json`에서 읽으므로 인자 없이 돌린다.

39장 전부 통과하며 주의도 없다.

## 접근성 (2026-08-14 감사)

정본은 글로벌 접근성 헌장이다. 이 덱에서 실측으로 확인하고 고친 것만 적는다.

**칩은 세로로 세운 목록이어야 한다.** 가로로 늘어놓으면 두세 개가 같은 시각 행에 묶이고, 스크린 리더 브라우즈 모드는 시각 행 단위로 읽어서 여러 항목을 한 번에 몰아 읽는다. 실측으로 2장의 칩 4개가 2행, 9장의 6개가 2행이었다. `<div>`와 `<span>`을 `<ul>`과 `<li>`로 바꾸고 `flex-direction:column`으로 세워 한 항목이 한 줄을 차지하게 했다. `<ul role="list">`는 쓰지 않는다.

**랜드마크를 두지 않는다.** 한 화면에 슬라이드 한 장뿐이고 컨트롤은 다섯 개라 건너뛸 반복 블록이 없다(WCAG 2.4.1 미성립). `<main>`을 두면 장을 넘길 때마다 포커스가 그 안으로 들어가 "main 시작"이, 끝까지 읽으면 "main 끝"이 마흔여섯 번 반복된다. 얻는 것 없이 통지만 두 배가 되므로 `<div>`로 바꿨다. 남은 `role`은 차트 세 개의 `role="img"`뿐이고, 이것은 막대와 도넛을 하나의 설명된 객체로 묶는 정당한 용도다.

**토글 버튼의 상태 채널은 라벨 하나다.** 라벨이 「내레이션 켬」과 「내레이션 끔」으로 바뀌므로 `aria-pressed`를 함께 두면 상태를 두 번 말한다. `aria-pressed`를 뺐다. 문서 모드 버튼과도 같은 방식이 됐다.

**슬라이드 번호는 스크린 리더에도 보인다.** 이전과 다음 버튼 사이의 「3 / 46」에 `aria-hidden`이 붙어 있어 시각 사용자만 볼 수 있었다. 뗐다. 장을 넘길 때 자동으로 읽어 주지는 않는다. 포커스가 제목을 읽고 내레이션이 이어지는 자리에 통지를 하나 더 얹으면 겹친다.

**장을 넘길 때 포커스는 그 장의 첫 요소로 간다.** 제목만 잡으면 그 위에 있는 눈썹줄(날짜·장소·번호)이 앞으로 읽어 나가는 흐름에서 통째로 빠진다. 46장 가운데 23장이 눈썹줄로 시작하므로 절반이 첫 줄을 잃는다. 모든 슬라이드가 눈썹줄이나 제목으로 시작하니 `firstElementChild`가 곧 그 장의 첫 줄이다. 7장은 「2023년 겨울, 암스테르담」, 37장은 「하나」를 먼저 읽고 다음 누름에서 제목으로 간다.

**단계별 등장은 DOM 순서 그대로다.** 실측으로 확인했다. 진입 시 제목에 포커스가 가고 조각은 0개, 이후 한 번 누를 때마다 다음 조각이 등장하며 포커스가 그 조각으로 옮겨 간다. 마지막 조각 뒤 한 번 더 누르면 다음 장 제목으로 간다.

**미디어는 vh로 못 박지 않는다.** 사진과 영상 높이를 `50vh`·`56vh`로 두었더니 창 높이 674px에서 다시 넘쳤다. `.slide.active`를 세로 flex로 두고 미디어 블록이 남은 높이를 차지하게 한 뒤 그 안에서 이미지가 줄어들게 했다(`min-height:0`이 없으면 flex 항목은 콘텐츠 크기 아래로 줄지 않는다). 줄어들 곳이 없는 칩·차트는 `@media (max-height:760px)`에서 여백만 조인다. 글자 크기와 수치는 건드리지 않는다.

## 손대지 말아야 할 것

엔진은 `lecture-deck` 스킬 템플릿에서 왔다. 아래 셋은 실패를 겪고 되돌린 자리다.

| 코드 | 이유 |
|---|---|
| `turn.volume = 1`, `narrAudio.volume = 1` | 낮추면 스크린 리더 음성에 묻힌다 |
| `NARR_DELAY = 1100` | 0으로 만들면 제목 낭독과 정면으로 겹친다 |
| `.fragment{visibility:hidden}` | `opacity:0`만으로는 DOM에 살아 있어 Tab이 등장 전 링크를 잡는다 |
| 차트 CSS 뒤의 「특이도 보정」 블록 | 지우면 마지막 문장이 본문 크기로 줄고 34장이 화면을 넘친다 |

특이도 보정은 템플릿에서 물려받은 버그를 막는 것이다. `.slide p,.slide li`가 특이도 (0,1,1)이라 한 클래스로 선언한 `<p>` 규칙(`.huge-q`, `.big-quote`, `.eyebrow`, `.byline`, 차트 문구 전부)을 이긴다. 실측으로 `.huge-q`가 41px 대신 21px로 나왔고 막대 하나가 58px 대신 109px로 부풀어 34장이 226px 넘쳤다. 같은 값을 `.slide` 접두사로 다시 선언해 되돌렸다. 이 덱만의 문제가 아니라 템플릿 쪽 결함이므로 다음 덱에서도 확인한다.

이 덱에서 엔진에 더한 것은 둘이다. 슬라이드를 넘길 때 앞 장의 영상을 정지시키고, 영상이 재생되면 내레이션을 멈춘다. 영상 위에서는 방향키와 스와이프가 슬라이드를 넘기지 않는다.

## 미결 사항

- **내레이션 미생성.** `_generate_narration.py`에 46장 문안이 들어 있고 mp3만 없다. 청중에 시각장애인 구성이 확인되지 않아 옵션으로 남겼다. 생성하면 ElevenLabs 유료 호출이 46건 발생한다
- **GitHub 저장소·Pages 배포 안 함.** 본인 사진과 언론사 사진이 들어 있어 공개 배포 여부를 확정하지 않았다
- **경기장 사진 교체.** 2026년 초 스페인 여행 사진에서 실물을 찾으면 `img/stadium-boot.jpg`를 대체한다
- **현장 확인.** 프로젝터 음향 출력과 인터넷 회선. 영상은 폴더 안에 있어 회선과 무관하지만 음향 출력은 필요하다
